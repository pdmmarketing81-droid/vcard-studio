// Superseded by src/components/admin/CardActions.tsx, which groups Delete with
// Duplicate and PublishToggle. Re-exported here so nothing has two copies of
// the same logic. Safe to delete along with this comment.
export { DeleteButton as default } from './admin/CardActions';
