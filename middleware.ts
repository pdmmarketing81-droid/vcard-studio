// ⚠️ NOT THE REAL MIDDLEWARE — see src/middleware.ts
//
// This project uses a src/ directory, so Next.js only picks up middleware at
// src/middleware.ts. A middleware file at the repo root is silently ignored:
// it compiles nothing, throws no error, and custom domains just quietly stop
// working. This placeholder exists purely so nobody re-creates that bug.
//
// Edit src/middleware.ts instead. This file can be deleted safely.

export {};
